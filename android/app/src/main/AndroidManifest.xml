<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Envio para o Google Drive e login com a conta Google. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Permissões que NÃO queremos, removidas na fusão dos manifestos.

         O `open_filex` declara acesso de leitura à galeria inteira (imagens,
         vídeos e até áudio) e o `video_compress` declara escrita no
         armazenamento externo. Nenhuma delas é usada aqui: as fotos entram
         pelo seletor do sistema (`image_picker`) e os documentos pelo SAF
         (`file_picker`), e nenhum dos dois exige permissão.

         Sem esta remoção o aplicativo apareceria na loja pedindo acesso a
         toda a biblioteca de fotos, vídeos e música — o oposto da promessa
         que ele faz — e cairia na política de Photo and Video Permissions do
         Google Play, que recusa quem poderia ter usado o seletor. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>

    <!-- Backup desligado. Nada do que fica no aparelho precisa sair dele: o
         cache do Firestore (que guarda o texto das cartas), as miniaturas e
         os downloads são todos recuperáveis do Drive na próxima entrada.
         Desligar evita que essa cópia viaje no backup automático do Android e
         na transferência entre aparelhos.

         `dataExtractionRules` é o formato do Android 12+; `fullBackupContent`
         cobre as versões anteriores. Os dois apontam para "não copie nada". -->
    <application
        android:label="Meu Bebê"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
