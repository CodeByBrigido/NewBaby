{
  "name": "meu-bebe-regras",
  "private": true,
  "type": "module",
  "description": "Verificação das regras de segurança do Firestore contra o emulador oficial.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-meubebe --config ../firebase.json \"node regras.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.0.0"
  }
}
